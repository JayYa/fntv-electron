# 领域词汇表

本文件定义本项目在讨论"看剧进度"这一片领域时使用的术语。只写概念，不写实现。

## 已观看 (watched)

播放项级别的完成标记，表达"这一集看完了"。

已观看与[播放进度](#播放进度-play-record)是**两个独立的概念**：前者回答"看完了没"，后者回答"看到哪儿了"。一个播放项可以有播放进度而未被标记为已观看，两者互不推导。

本客户端只**置位**已观看，从不清除它。服务端没有提供取消已观看的接口；用户跳回某一集重看，也不会让它变回未观看。

## 播放进度 (play record)

某个播放项当前看到的时间点，以及该播放项的总时长。它是服务端"继续观看"功能的依据，也是下次打开这一集时决定从哪里接着播的依据。

## 接近片尾 (near end)

播放比例达到或超过 **98%**。

这是**一个**概念、**一个**阈值，同时服务于两处判断：

1. 决定**要不要恢复上次播放位置**——已经接近片尾的播放项，重新打开时从头播，而不是跳到几乎结束的位置。
2. 决定**算不算看完了**——离开一个播放项时，若其最终进度接近片尾，则将其标记为已观看。

两处共用同一个阈值，避免出现"一集被认为太接近片尾所以不恢复进度，却又不够接近片尾所以不算看完"的矛盾。

当播放项的总时长缺失或不可信时，不视为接近片尾。宁可漏判，也不误判。

## 播放项 (PlayItem)

播放列表中的一集，由 `itemGuid` 标识。

区别于 `mediaGuid`：播放项是用户视角的"一集"，而 `mediaGuid` 指向承载这一集的具体媒体文件。同一个播放项可能对应多个媒体文件（不同清晰度、不同来源）。
